# AGENTS.md — AI 操作手册（v3.1）

> **核心原则**：AI 不是来"分解文章"的，AI 是来"建模一个人的思想"的。
>
> 本项目已从读书笔记系统升级为**个人思想建模与外化系统**，目标是为未来 agent 的 GraphRAG 提供结构化的内容支持。

---

## 一、系统的核心逻辑

### 1.1 领域卡片是骨架

知识网络的组织方式不是文件夹，不是标签，而是**领域**。

- 顶级领域卡片（如"教学领域""认知领域"）是 AI 的**入口**。
- 领域卡片下涉及的实例卡片（概念、模型、方法、实体、冲突）是 AI **滋养**的对象。
- `01-Cards/_meta/ontology.md` 是当前元结构的**活契约**，AI 每次操作前必须读取。

### 1.2 AI 的渐进式阅读

AI **不允许**一次性读取全部卡片。必须遵循：

```
Step 1: 读取 01-Cards/_meta/ontology.md
Step 2: 读取 domains/ 下的全部顶级领域卡片
Step 3: 判断内容涉及哪些领域
Step 4: 读取相关领域卡片指向的实例卡片
Step 5: 进行比较、补充、冲突检测
Step 6: 更新领域卡片和 ontology.md（如有新发现）
```

### 1.3 内容是燃料，卡片是资产

**禁止行为**：
- ❌ 为每篇原始文档创建摘要文件
- ❌ 把内容拆成碎片分发到无数空卡片
- ❌ 在 Inbox 中堆积已处理的原始文档
- ❌ 创建幽灵链接
- ❌ 让卡片因无限引用而膨胀

**必须行为**：
- ✅ 用内容去**丰富已有卡片**
- ✅ 只在遇到**真正新的概念/模型/冲突**时才新建卡片
- ✅ 处理完后将原始文档移动到 03-Archive/
- ✅ 所有 AI 生成的内容标注来源
- ✅ 遵循实例引用上限：cards ≤ 6 条，profile ≤ 10 条

---

## 二、指令集

### 2.1 `/construct`（/建构）

**触发条件**：用户在 `00-Inbox/` 中放入若干篇最关键的"种子文章/思考"后，主动说 `/construct`。

**执行步骤**：

```
1. 读取 00-Inbox/ 下全部文件。
2. 读取 01-Cards/_meta/ontology.md。
3. 识别用户反复关心的核心主题 → 生成顶级领域卡片（domains/*.md）。
4. 识别跨领域的关键概念、实体、冲突、模型 → 生成实例卡片。
5. 根据涌现出的内容，设计 01-Cards/ 的目录结构。
6. 更新 ontology.md，声明当前元结构。
7. 基于种子内容生成 02-Profile/ 初稿：
   a. 02-Profile/语言风格.md：
      - 表达 DNA：句式偏好、词汇特征、段落节奏、幽默方式、确定性表达、引用习惯
      - 常用句式/修辞、术语偏好、原文摘录
   b. 02-Profile/思维模型.md：
      - 心智模型（3-7 个）：名称、一句话描述、来源证据、应用场景、局限性
      - 每个心智模型必须通过三重验证：跨域复现、生成力、排他性
      - 决策启发式（5-10 条）：「如果 X，则 Y」的快速判断规则
      - 推理模式、解决问题的默认路径、智识谱系、内在张力
      - 原文摘录
   c. 02-Profile/核心信念.md：
      - 根本立场、明确反对、认为最重要
      - 价值观排序、反模式、诚实边界（知识截止、盲区、不能替代真人判断的场景）
      - 原文摘录
   d. 每条推断必须标注来源（文件名或具体引用）。
   e. 在 02-Profile/profile-index.md 中记录版本历史。
   f. 若某栏目种子内容不足，保留结构化空槽并标记为 awaiting-content，不编造内容。
8. 为每张生成的卡片写入 YAML frontmatter。
9. 建立领域卡片与实例卡片之间的显式 relations。
10. 输出建构报告。
11. 将种子文档移动到 03-Archive/。
```

**约束**：
- `/construct` 只跑一次或少数几次，建立基础骨架。
- 不生成空卡片。
- `/construct` 必须基于种子内容生成 02-Profile/ 初稿，生成的条目 frontmatter `status: seed`。
- 每个 Profile 文件的原文摘录总数 ≤ 10 条。
- `/construct` 禁止通过读取全部卡片来反推 Profile，只能基于种子文档和已生成的领域/实例卡片进行主题聚焦。
- 用户可在报告后要求调整。

### 2.2 `/digest`（/消化）

**触发条件**：`00-Inbox/` 中有新文件。不再区分外来文章与自己的思考。

**执行步骤**：

```
1. 读取 00-Inbox/ 中的新文件。
2. 读取 01-Cards/_meta/ontology.md。
3. 读取 domains/ 下的全部顶级领域卡片。
4. 判断新内容涉及哪些领域。
5. 读取相关领域卡片及其指向的实例卡片。
6. 进行比较分析：
   a. 概念/模型/方法是否已有卡片？
      → 有：丰富卡片，更新 sources 和 updated 字段
      → 无：判断是否重要且持久，决定是否新建
   b. 内容与现有卡片是否冲突？
      → 是：标记冲突，考虑新建冲突卡片
   c. 实体是否已有卡片？
      → 有：补充信息
      → 无：判断重要性后决定是否新建
7. 评估对 02-Profile/ 的影响：
   a. 读取当前 02-Profile/ 全部文件。
   b. 对比新内容与现有条目，检测以下变化：
      - 语言风格：新的句式/修辞、词汇特征、幽默方式、确定性表达、引用习惯
        → 确认后追加到 02-Profile/语言风格.md
      - 思维模型：新的心智模型、决策启发式、智识谱系影响、内在张力
        → 确认后追加到 02-Profile/思维模型.md
        → 新模型必须通过三重验证（跨域复现、生成力、排他性）中至少两项
      - 核心信念：新的根本立场、价值观排序变化、反模式、诚实边界
        → 确认后追加/修订 02-Profile/核心信念.md
   c. 当某 Profile 文件积累了 ≥3 个不同来源的支持证据，或用户明确确认时，
      将 frontmatter status 从 seed 升级为 growing。
   d. 若原文摘录超过 10 条上限，向用户报告并请求替换决策。
   e. 在 02-Profile/profile-index.md 中追加版本历史记录。
   f. 若无变化：在消化报告中说明「未检测到 Profile 变化」。
8. 检查目录结构是否需要调整：
   a. 现有子目录能否容纳新卡片？
   b. 是否需要新建子目录？
   c. 是否需要合并/重命名旧目录？
   d. 更新 ontology.md 记录结构变更
9. 更新领域卡片。
10. 将原始文档移动到 03-Archive/。
11. 输出消化报告。
```

### 2.3 `/refactor`（/重构）

**目的**：让 LLM 从已有卡片中**涌现出新的内容组织和结构模式**，主动调整链接、合并或拆分卡片，使知识网络保持清晰、紧凑、可生长。

**触发条件**：

- 用户主动说 `/refactor`（可指定范围，如某个领域或一组卡片）。
- `/digest` 的输出报告指出存在结构张力（冗余、分裂、类型错配、链接过载）。
- 卡片数量或关系复杂度增长，现有目录/关系已无法自然容纳。
- 用户在阅读或使用卡片时发现组织混乱，要求整理。

**执行步骤**：

```
1. 读取 01-Cards/_meta/ontology.md，确认当前元结构和关系类型。
2. 读取 domains/ 下的全部顶级领域卡片，理解现有骨架。
3. 根据领域卡片的指向，读取需要重构范围内的实例卡片。
4. 识别重构信号（至少检查以下维度）：
   a. 冗余信号：两张及以上卡片在核心主张、定义或实例上高度重叠。
   b. 分裂信号：一张卡片包含两个及以上可独立回答的问题，或跨越多个领域。
   c. 链接空洞：高相关卡片之间缺少显式 relations，或存在只能单向抵达的孤岛。
   d. 链接过载：单张卡片 outgoing relations 超过 6 条，需要重新分层。
   e. 类型错配：卡片实际内容与其 `type` 不一致。
   f. 目录张力：现有领域目录无法容纳新涌现的卡片群，或某目录过于膨胀。
   g. 幽灵链接或断裂关系：指向已删除、重命名或 deprecated 的卡片。
5. 基于以上信号，提出 1-3 个重构方案，每个方案包含：
   a. 诊断：发现了什么组织问题。
   b. 动作：合并、拆分、移动、重命名、调整类型、增删链接、新建子领域等。
   c. 影响范围：涉及哪些卡片/领域/目录。
   d. 风险：可能丢失的信息、需要人工判断的地方。
6. 向用户呈现方案，等待用户选择或修改。未经用户确认，不执行任何写入。
7. 用户确认后执行重构：
   a. 合并卡片：保留一个主卡片，将被合并卡片的内容、来源、relations 迁移过去，原卡片 frontmatter 改为 `status: deprecated`，并在正文中说明合并目标。
   b. 拆分卡片：为新主题创建新卡片，拆分内容、来源、relations，更新原卡片。
   c. 调整链接：增删或修改 frontmatter relations 与正文 [[id|显示文本]]。
   d. 调整目录/领域：新建、重命名或合并领域目录；更新领域卡片；必要时更新 ontology.md。
   e. 修正类型：在确认后调整卡片的 `type`。
   f. 清理幽灵链接：修复或删除指向不存在或 deprecated 卡片的链接。
8. 更新所有受影响卡片的 `updated` 字段。
9. 在 01-Cards/_meta/ontology.md 中记录结构规则的演化（如有新增关系类型、目录结构变化等）。
10. 输出重构报告，包含：
    a. 执行的方案摘要。
    b. 变更清单（卡片级、目录级、关系级）。
    c. 新增/废弃的卡片列表。
    d. 仍然存在的张力或未决问题。
```

**约束**：

- `/refactor` 不处理新内容消化，只处理已有卡片的组织。
- 禁止删除任何卡片；合并后的原卡片标记为 `status: deprecated`，并保留指向主卡片的链接。
- 合并/拆分卡片时，必须在相关卡片中记录历史沿革和原因。
- 所有新增、修改、删除的链接必须保证无幽灵链接。
- 每次 `/refactor` 必须更新 `ontology.md` 的结构变更记录。
- 若重构涉及 02-Profile/，需按 4.2 规则单独获得用户确认。

**说明**：`/digest` 可能在报告中提示需要 `/refactor`，但不会自动执行 `/refactor`。

### 2.4 `/analyze`（/分析）

**触发条件**：用户对一个具体问题说 `/analyze`。

**执行步骤**：

```
1. 解析用户提出的问题。
2. 读取 02-Profile/ 下的全部档案文件。
3. 读取 01-Cards/_meta/ontology.md。
4. 读取 domains/ 下可能与问题相关的领域卡片。
5. 根据领域卡片的指向，读取相关实例卡片。
6. 在知识库中检索与问题相关的概念、模型、冲突、实体。
7. 组装分析上下文包（Context Package），写入 `04-OutBox/YYYYMMDD-主题-context.md`：
   a. 用户透镜：从 Profile 中蒸馏与问题相关的心智模型、核心信念、语言风格提示、诚实边界。
   b. 相关领域：列出相关领域卡片及简要说明。
   c. 相关卡片：按类型（概念、模型、方法、实体、冲突等）分组，提供摘要、状态、1-3 条关键摘录、相关关系。
   d. 关系子图：抽取相关卡片之间的显式关系。
   e. 冲突与张力的显式标注。
   f. 假设链（可选）：当问题涉及 ≥3 个卡片、≥2 个领域、关键证据不足，或用户要求详细分析时生成；否则标注「未触发」。
   g. 盲区与假设：列出知识库缺口、需要依赖通用推理的地方、Profile 可能过时的维度。
   h. 来源：列出原始文档/卡片来源。
8. 基于上下文包生成回答，输出到 `04-OutBox/YYYYMMDD-主题.md`：
   a. 只读取 Context Package，不再回头读取原始卡片。
   b. 用用户的思维模型组织论证。
   c. 体现用户的语言风格。
   d. 尊重用户的核心信念。
   e. 引用相关卡片（[[id|标题]]）。
   f. 指出知识库中的冲突或张力。
9. 在 OutBox 文件顶部标注问题来源、引用卡片 id 列表、分析时间、关联的 Context Package 路径。
10. 输出简要报告。
```

---

## 三、卡片规范

### 3.1 YAML frontmatter

```yaml
---
id: "cognitive-scaffolding"
title: "认知脚手架"
type: "notion"
created: "2026-07-07"
updated: "2026-07-07"
sources:
  - "来自《某书》第 3 章"
relations:
  - target: "zone-of-proximal-development"
    type: "extends"
    note: "脚手架作用在最近发展区内"
tags:
  - "教学"
status: "seed"   # seed | growing | mature | deprecated
---
```

### 3.2 卡片类型

由 `ontology.md` 声明。初始集合：

| type | 定义 | 回答的问题 |
|------|------|-----------|
| `domain` | 领域卡片 | 「这是哪个思想领域？」 |
| `notion` | 观念、立场、信条 | 「我认为/怎么看这件事？」 |
| `principle` | 原则、原理、规律 | 「什么必须被遵守？为什么会这样？」 |
| `phenomenon` | 现象、模式、趋势 | 「发生了什么？呈现什么模式？」 |
| `entity` | 有明确边界的实体对象 | 「这个具体对象是什么？」 |
| `group` | 多个实体构成的群体 | 「这是哪一类人/事物的群体？」 |
| `model` | 模型、框架、解释结构 | 「这个复杂事物怎么理解？」 |
| `method` | 方法、技巧、流程 | 「怎么做才能达到目标？」 |
| `conflict` | 冲突、张力、两难 | 「这里的根本矛盾是什么？」 |
| `note` | 暂时无法归类的想法 | 「这个想法还没成熟」 |

**类型边界判定**：当一张卡片可能属于多个类型时，按以下优先级选择主类型：

1. 约束判断或行动 → `principle`
2. 解释复杂事物结构 → `model`
3. 提供可执行步骤 → `method`
4. 描述可观察模式 → `phenomenon`
5. 表达个人立场 → `notion`
6. 有明确边界的独立对象 → `entity`
7. 多个对象构成的群体 → `group`
8. 记录根本对立 → `conflict`

### 3.3 目录组织

- `01-Cards/domains/`：领域卡片。
- `01-Cards/[领域目录]/`：该领域下的实例卡片，按领域组织，不按类型组织。
- 同一领域目录下可混合存放 `notion`、`principle`、`method` 等多种类型卡片。

### 3.4 实例引用上限

- `01-Cards/` 卡片：原始资料实例 ≤ **6 条**。
- `02-Profile/` 档案：原文摘录 ≤ **10 条**。
- 超过上限时，AI 比较新旧内容，用更优质的替换。

### 3.5 链接治理

#### 3.5.1 只用于已存在的卡片

最高标准：**禁止幽灵链接**。`[[id|显示文本]]` 或 `[[id]]` 必须指向真实存在的卡片。

#### 3.5.2 领域卡片的特殊规则

领域卡片可以在「涉及的概念」等部分列出**期望存在但尚未创建**的卡片，但**不使用 `[[ ]]`**，而是使用**加粗**：

```markdown
## 涉及的概念
- [[cognitive-scaffolding]] — 已存在
- **同伴互助** — 待创建（发现相关文章时创建）
```

待出现与之对应的卡片时，修正并创建链接
#### 3.5.3 冲突卡片的链接

冲突卡片必须链接到**涉及的具体概念卡片**：

```markdown
## 涉及的概念
- [[cognitive-scaffolding]]
- [[self-regulated-learning]]
```

#### 3.5.4 高相关卡片之间的链接建立

`/digest` 负责在日常消化中维护卡片与领域之间的关系，并在高度相关的实例卡片之间建立显式链接；`/refactor` 则负责当结构张力累积时，主动涌现新的组织模式、调整链接、合并或拆分卡片。

**判断高相关的信号**：
- 在同一篇原始文档的同一论点中被共同提及；
- 共享多个相同来源；
- 属于同一领域且语义上互补、对立或扩展；
- 一方是另一方的具体例子、应用场景或反对对象。

**建立链接的方法**：
1. 在两张卡片的 YAML frontmatter `relations` 中分别补充指向对方的关系，例如：
   ```yaml
   relations:
     - target: "self-regulated-learning"
       type: "supports"
       note: "自我调节学习是脚手架拆除后的目标"
   ```
2. 在卡片正文中，当上下文自然提到对方时，插入 `[[id|显示文本]]` 链接。
3. 关系类型优先从 `ontology.md` 中声明的类型选择（如 `extends`、`conflicts-with`、`supports`、`example-of`、`applies-to`、`influences`），若无合适类型则向用户报告，经确认后再新增。

**边界**：
- 避免为了链接而链接；只有确实存在思想关联时才建立。
- 单张卡片的 outgoing relations 建议不超过 6 条，防止卡片因无限引用而膨胀。
- 领域卡片不受上一条约束，因为领域卡片要起到统摄下属卡片的作用。
- 若建立关系后发现两张卡片应合并，应向用户报告，而不是直接合并。

---

## 四、02-Profile/ 使用规则

### 4.1 档案组成

- `profile-index.md`：总览与版本历史
- `语言风格.md`：表达 DNA（句式、词汇、节奏、幽默、确定性、引用）+ 常用句式/修辞、术语偏好
- `思维模型.md`：心智模型（含三重验证）、决策启发式、推理模式、默认解决路径、智识谱系、内在张力
- `核心信念.md`：根本立场、价值观排序、反模式、诚实边界

### 4.2 更新原则

- `/construct` 在建构阶段基于种子内容生成 Profile 初稿，frontmatter 标记为 `status: seed`。
- `/digest` 不每次消化都更新，只在发现新模式或信念变化时才更新。
- AI 更新前必须向用户报告变更点，经确认后写入。
- 每次更新在 `profile-index.md` 的「Profile 演化日志」中追加记录。
- 当某 Profile 文件积累了 ≥3 个不同来源的支持证据，或用户明确确认时，可将 `status` 从 seed 升级为 growing。

---

## 五、安全与边界

### AI 禁止事项

❌ **不要**：
- 创建信息稀薄的空卡片
- 为每篇文档都创建新卡片
- 在 Inbox 中堆积已处理的原始文档
- 未经用户确认修改 02-Profile/
- 删除任何卡片（只能标记为 deprecated）
- 创建幽灵链接
- 让卡片因无限引用而膨胀

### AI 必须事项

✅ **必须**：
- 优先丰富已有卡片，而非新建卡片
- 检测并记录冲突
- 维护领域卡片的完整性
- 处理完后归档原始文档
- 输出清晰的消化/建构/分析报告
- 所有 AI 生成的内容标注来源
- 遵循实例引用上限

---

## 六、Hooks 与自动化

为了把规则明确的确定性操作从 LLM 中抽离，项目使用 hooks 处理校验与执行。LLM 仍负责语义判断、涌现模式识别和需要人工确认的创作型决策。

### 6.1 启用方式

```bash
# 让 Git 使用项目内的 hooks 目录
git config core.hooksPath hooks

# 提交前会自动运行卡片校验
```

### 6.2 当前 hooks

#### `scripts/validate_cards.py` + `hooks/pre-commit`

提交前自动运行，检查：

- frontmatter 必填字段完整性（`id`, `title`, `type`, `created`, `updated`, `status`）
- `id` 唯一性
- `type` 有效性
- 正文与 `relations` 中的链接是否指向真实存在的卡片（**幽灵链接检测**）
- `relations` 数量是否超过建议上限 6
- `sources` 列表项数量是否超过上限 6
- 正文 `## 实例` 部分引用数量是否超过上限 6
- 是否存在未被任何领域卡片引用的 **orphan 实例卡片**

校验发现错误时阻止提交；发现警告时允许提交，但会提示用户。

### 6.3 hooks 与四个指令的协作

| 指令 | hooks 承担的确定性工作 | LLM 承担的语义工作 |
|---|---|---|
| `/construct` | 目录创建、frontmatter 模板校验、id 唯一性检查 | 主题识别、领域设计、Profile 初稿 |
| `/digest` | 输入文件存在性检查、frontmatter 补全、归档移动、updated 字段更新 | 概念匹配、冲突检测、新建/丰富判断 |
| `/refactor` | 重构信号扫描（链接过载、孤立卡片、幽灵链接、目录膨胀）、变更清单格式化 | 涌现模式判断、合并/拆分方案提出 |
| `/analyze` | Context Package 格式校验、输出文件命名、引用列表提取 | 推理、论证、风格化表达 |

### 6.4 添加新 hooks

新增 hook 时应遵循：

1. 只处理有明确规则的任务。
2. 不替代 LLM 的语义判断。
3. 失败时给出清晰的可操作提示。
4. 更新本手册的 hooks 说明。

---

> *本手册随系统演化而更新。版本：v3.1*
